import ProviderCalendarScreen from './ProviderCalendarScreen';

// Backward-compatible wrapper.
// Older navigation routes still open ProviderRequestsScreen, but the provider flow
// now lives in the unified My Jobs screen with Calendar / Ongoing / Requests / Quotes / History tabs.
export default ProviderCalendarScreen;
